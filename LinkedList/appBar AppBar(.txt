appBar: AppBar(
        title: const Center(child: Text("My tickets")),
      ),



Row(
                  children: [
                    const Icon(Icons.search, color: Color(0xFF8FC205)),
                    Text(
                      "Search",
                      style: Styles.headLineStyle4,
                    )
                  ],
                )